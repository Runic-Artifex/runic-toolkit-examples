{
  "name": "@runic-artifex/toolkit-examples",
  "private": true,
  "version": "0.0.0-private",
  "license": "MIT",
  "packageManager": "npm@11.16.0",
  "engines": {
    "node": "24.18.x",
    "npm": "11.16.x"
  },
  "workspaces": ["samples/03-SetupApplication/Frontend"],
  "scripts": {
    "generate:setup-contract": "node eng/generate-setup-contract.mjs",
    "verify:setup-contract": "node eng/generate-setup-contract.mjs --check",
    "build": "npm run build --workspace @runic-artifex/setup-application",
    "typecheck": "npm run typecheck --workspace @runic-artifex/setup-application",
    "test": "npm run test --workspace @runic-artifex/setup-application",
    "verify": "npm run verify:setup-contract && npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "effect": "3.22.1"
  }
}
