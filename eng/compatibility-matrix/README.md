# Compatibility matrix fixture

This fixture projects the `current`, `previous-supported`, and
`next-candidate` lanes directly from a clean, committed release authority and
records the authority revision, tree, and digest alongside the matrix.

It proves lane identity and exact authority binding only. It does not certify
platform coverage, upgrades, interoperability, or any W70 gate.
