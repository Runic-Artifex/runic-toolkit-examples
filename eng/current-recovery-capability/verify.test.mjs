import assert from "node:assert/strict";
import test from "node:test";
import { verifyReceipt } from "./verify.mjs";

const provenance = { packages: ["Runic.Application", "Runic.Application.Testing", "Runic.Assets"].map((identity, index) => ({ identity, version: index === 2 ? "0.1.0-preview.8d22423" : "0.2.0-preview.w50002", archive: `${identity}.nupkg`, sha256: "a".repeat(64) })), editor: { binary: "RunicTranslations.Editor.dll", sha256: "b".repeat(64) } };
const journey = () => ({ schema: "runic.recovery-capability-consumer/1", isolation: { dotnetCliHome: ".dotnet", nugetPackages: ".nuget/packages", nugetHttpCache: ".nuget/http" }, projectReferences: [], packages: provenance.packages, editor: provenance.editor, capabilityGates: ["undeclared", "unconfigured", "mismatched"], lifecycleOutcomes: ["normal", "wait-primary", "start-primary", "cancellation"], recovery: { modes: ["complete", "rollback"], blockedMutations: 2, staleSessionReplays: 2, diagnostics: "sanitized-counts" }, diagnostics: { schema: "runic.translations.editor-diagnostics/1", outboundTransportAttempts: 0 }, phases: ["restore", "build", "test-host", "editor-smoke", "editor-diagnostics"].map(name => ({ name, status: "passed", exitCode: 0 })) });
test("accepts exact local recovery and capability evidence", () => assert.equal(verifyReceipt({ schema: "runic.recovery-capability-consumer-repeat/1", journeys: [journey(), journey()] }, provenance).ok, true));
test("rejects forged or softened recovery evidence", () => { const receipt = { schema: "runic.recovery-capability-consumer-repeat/1", journeys: [journey(), journey()] }; receipt.journeys[1].packages[0].sha256 = "f".repeat(64); receipt.journeys[1].recovery.staleSessionReplays = 0; receipt.journeys[1].capabilityGates.pop(); assert.equal(verifyReceipt(receipt, provenance).ok, false); });
