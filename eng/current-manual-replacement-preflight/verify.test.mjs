import assert from "node:assert/strict"; import test from "node:test"; import { verifyReceipt } from "./verify.mjs";
const p={executable:{sha256:"a".repeat(64)},candidateReceipt:{sha256:"b".repeat(64)},authority:{sha256:"c".repeat(64)}},j=()=>({schema:"runic.manual-replacement-preflight-consumer/1",result:"manual-replacement-eligible",guidance:"user-performed-verified-manual-replacement",networkAttempts:0,processMutations:0,hostileDiagnostics:["RID-MISMATCH","ARCHIVE-RECEIPT-MISMATCH","CANDIDATE-MODEL-STALE","CANDIDATE-PLATFORM-MISMATCH"],provenance:p});
test("accepts offline manual replacement evidence",()=>assert.equal(verifyReceipt({schema:"runic.manual-replacement-preflight-consumer-repeat/1",journeys:[j(),j()]},p).ok,true));
test("rejects updater-like or forged evidence",()=>{const r={schema:"runic.manual-replacement-preflight-consumer-repeat/1",journeys:[j(),j()]};r.journeys[1].networkAttempts=1;r.journeys[1].guidance="download and install";assert.equal(verifyReceipt(r,p).ok,false);});
