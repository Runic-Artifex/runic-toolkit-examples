import assert from 'node:assert/strict';
import test from 'node:test';
import { verifyReceipt } from './verify.mjs';

const journey = () => ({ schema: 'runic.native-shell-capability-certification/1', isolation: { inputs: 'isolated-copy-plus-explicit-abi-root', nuget: '.nuget/packages', dotnetCliHome: '.dotnet' }, freezeReceipt: { sha256: 'a'.repeat(64), schema: 'runic.controlled-nonpublic-profile-freeze-repeat/1' }, observedProfile: { certifiedResult: { status: 'unavailable', faultCode: 'REDIT0008', capability: 'private-file-handler-streaming-unavailable', actionablePrerequisite: 'webview-prerequisite-missing', deterministic: true } }, abiOracle: { status: 'passing', verification: 'pinned-nix-abi-check', csWebUi: { revision: '90648d7783681b9cbc4460f43ca15f3d7283f5a2', tree: '83736bfd4b61ffe8d3abf7ebb07203897a183a1c', webuiRevision: '52f9e75b92faf9a23fd150b3c60051c4ec85fc69' }, requiredSymbol: 'webui_set_icon_file', action: 'none' }, nonClaims: ['platform-success', 'browser-launch', 'file-read', 'browser-visual-e2e', 'public-listener', 'cross-platform-certification', 'os-upgrade-certification', 'c-webui-abi-change', 'signing', 'publication', 'updates'], externalActions: { requests: 0, browserLaunches: 0, publicListeners: 0, releases: 0, uploads: 0, signatures: 0 } });
test('accepts deterministic managed-refusal certification', () => { const value = journey(); assert.deepEqual(verifyReceipt({ schema: 'runic.native-shell-capability-certification-repeat/1', journeys: [value, structuredClone(value)] }, value), { ok: true, errors: [] }); });
test('rejects a softened refusal, forged ABI evidence, or platform claim', () => { const first = journey(), second = journey(); second.observedProfile.certifiedResult.status = 'available'; second.abiOracle.csWebUi.webuiRevision = 'f'.repeat(40); second.nonClaims.shift(); const report = verifyReceipt({ schema: 'runic.native-shell-capability-certification-repeat/1', journeys: [first, second] }, first); assert.equal(report.ok, false); assert.match(report.errors.join('\n'), /two deterministic|native-shell certification boundary/); });
