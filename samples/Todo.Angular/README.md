# Angular Todo

Runs both Todo demos through `@webuitoolkit/mvvm-angular`.

```console
dotnet run --project samples/Todo.Angular
dotnet run --project samples/Todo.Angular -- --advanced
```

See the [framework sample guide](../Todo.Frontends/README.md) for architecture,
verification, and comparison notes.
